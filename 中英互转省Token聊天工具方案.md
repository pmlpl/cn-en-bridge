# 中英互转省 Token 聊天工具方案

## 一、背景与痛点

在使用自建 Agent 接入自有模型进行中文聊天时，存在一个明显的痛点：

**中文输出 token 消耗过高。**

原因在于：
- 中文字符在主流模型的分词器（Tokenizer）中，往往被拆成多个 token，同样语义下中文消耗的 token 数明显多于英文。
- Agent 在思考、推理、输出长文本时，中文的 token 成本会成倍放大。
- 对于按 token 计费或受限于上下文窗口的场景，这会直接推高成本、压缩可用上下文。

因此，希望在不改变用户「用中文对话」体验的前提下，通过中英互转的方式降低 token 消耗。

## 二、方案概述

核心思路：**对用户透明地完成中英互转，让模型始终用英文"思考"和"输出"。**

整体流程如下：

```
用户输入中文
    ↓ （中文 → 英文翻译）
英文文本发送给 AI 模型
    ↓ （模型用英文推理并输出英文回复）
AI 输出英文
    ↓ （英文 → 中文翻译）
中文文本展示给用户
```

这样模型侧全程处理英文，token 消耗显著下降，而用户侧仍然是纯中文交互，体验不变。

## 三、工作流程详解

### 1. 用户输入阶段
- 用户在输入框输入中文内容。
- 工具在发送前，先将中文翻译为英文。

### 2. 模型交互阶段
- 将翻译后的英文作为 prompt 发送给 AI 模型。
- 模型以英文进行理解和生成，返回英文回复。

### 3. 结果展示阶段
- 接收到模型的英文输出后，再将其翻译为中文。
- 将最终的中文结果展示给用户。
- 可选：同时展示英文原文，便于对照和调试。

### 4. 上下文管理
- 历史对话可以英文形式保存，保持上下文连贯，同时继续节省 token。
- 如需向用户展示历史，可按需即时翻译为中文。

## 四、技术选型

### 后端
- **Web 框架**：FastAPI（原生 async/await，适合 I/O 密集的翻译+模型调用场景）
- **通信方式**：FastAPI 原生 WebSocket（实现实时双向通信，支持流式输出）
- **运行环境**：仅 CPU 即可运行，无需 GPU

> 选型说明：本工具的核心操作（中→英翻译、调用 AI 模型、英→中翻译）均为 I/O 密集型，FastAPI 的异步特性可在模型推理等待期间不阻塞其他请求，吞吐量优于同步框架（如 Flask）。且 WebSocket 与流式输出在 FastAPI 中开箱即用。

### 翻译引擎（可选方案）
| 方案 | 优点 | 缺点 |
| --- | --- | --- |
| 本地开源模型（如 CSANMT / NLLB / Opus-MT） | 无云端依赖，数据不出本地，长期使用成本低 | 翻译质量相对一般，需自行部署维护 |
| 云端翻译 API（如 DeepL、百度翻译、Google Translate） | 翻译质量高，接入简单 | 有调用成本，数据需出站 |
| 直接用大模型自身翻译 | 无需额外组件，质量好 | 会额外消耗 token，削弱省 token 的初衷 |

> 建议：优先使用轻量本地翻译模型，在保证翻译质量的前提下最大化省 token 效果。

### 前端
- 双栏对照界面：
  - 左栏：用户输入中文
  - 右栏：实时显示 AI 的英文回复及最终中文翻译
- 支持流式输出，提升交互体验。

## 五、预期效果

1. **Token 显著下降**：同等语义下，英文 token 数通常为中文的 1/2 ~ 2/3，整体消耗可大幅降低。
2. **用户体验不变**：用户全程用中文，无额外学习成本。
3. **可本地运行**：不依赖 GPU，部署门槛低。
4. **可扩展**：后续可接入更多语种或替换翻译引擎。

## 六、风险与注意事项

1. **翻译失真**：中英互转会带来语义损失，尤其在专业术语、口语化表达、文化相关内容上，需关注翻译质量。
2. **延迟增加**：多了两次翻译环节，整体响应延迟会上升，需评估是否可接受。
3. **上下文一致性**：历史以英文保存时，要注意翻译误差是否会被放大；必要时可保留关键中文摘要。
4. **翻译成本**：若使用云端 API，需权衡翻译费用与省下的 token 费用是否划算。
5. **隐私合规**：涉及用户内容翻译出站时，需符合相关数据合规要求。

## 七、后续行动

- [ ] 完成关于中英互转省 token 聊天工具的方案文档撰写
- [ ] 评估并选定翻译引擎方案
- [ ] 设计前后端接口与数据流
- [ ] 搭建最小可用原型并验证省 token 效果
